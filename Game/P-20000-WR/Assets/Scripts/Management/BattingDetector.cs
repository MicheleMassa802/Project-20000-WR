using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattingDetector : MonoBehaviour
{

    // to handle what is considered a hit, homer, etc... and set up its respective event

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
